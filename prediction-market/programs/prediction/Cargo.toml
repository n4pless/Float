[package]
name = "prediction"
version = "0.1.0"
description = "SOL Prediction Market — PancakeSwap-style"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "prediction"

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
